# Python-зависимости пайплайна (исполняются вне Blender — анализ/калибровка/валидация).
# Blender-скрипты используют встроенный в Blender Python и bpy, им этот файл не нужен.

numpy>=1.26
trimesh>=4.0          # измерение мешей, срезы, периметры (виртуальная лента)
pygltflib>=1.16       # чтение/валидация GLB вне Blender (проверка морфов, бюджетов)
pyyaml>=6.0           # product.yaml в контентном конвейере (этап 6)
