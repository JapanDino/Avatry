# Текущее состояние проекта

> Обновляется в конце каждой рабочей сессии (3–6 строк). Это первое, что читает ИИ в новой сессии.

**Этап:** 0 (каркас репозитория) — **завершён**. Следующий — этап 1 (параметрическое тело).

**Сделано в последней сессии (2026-06-11):** этап 0 целиком. Структура монорепы; web/ на Vite + React + TS + Three.js (react-three-fiber) — сцена с кубом, сеткой и OrbitControls собирается и рендерится (проверено скриншотом `artifacts/review/stage0-scene.png`); pipeline/ с `morphs.py` (канонический словарь морфов) и `requirements.txt`; `.gitignore`, `.gitattributes` (LFS), `README.md`, `assets/SOURCES.md`. До этого — вся проектная документация и ТЗ.

**Следующий шаг:** задача 1.1 из PLAN.md — установить Blender (LTS), зафиксировать путь к `blender.exe` ниже. Затем 1.2 (MPFB2). Это требует действий пользователя (скачать/установить Blender) либо разрешения ставить через winget.

**Блокеры:** Blender на машине ещё нет (нужен с задачи 1.1).

**Окружение:** Windows 11, PowerShell. Node 22.22, npm 10.9, Python 3.13.5, git-lfs 3.7. Путь к blender.exe: _(заполнить после установки)_.
**web dev-сервер:** `cd web && npm run dev -- --host 127.0.0.1` (на Windows IPv4-хост обязателен, иначе Vite слушает только `::1`).
