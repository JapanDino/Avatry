# server/

Бэкенд (FastAPI + PostgreSQL + MinIO) появляется на **этапе 5** плана.

До этого этапа приложение работает полностью на клиенте: аватар считается в браузере,
состояние хранится в localStorage. Это осознанное решение (см. `docs/DECISIONS.md`).
